// Source code is decompiled from a .class file using FernFlower decompiler (from Intellij IDEA).
package oops;

class StudentReportCard {
   long rollN;
   String name;
   int m1;
   int m2;
   int m3;
   int m4;
   int m5;
   int total;
   float avg;
   String grade;

   StudentReportCard(long rollN, String name, int m1, int m2, int m3, int m4, int m5) {
      this.rollN = rollN;
      this.name = name;
      this.m1 = m1;
      this.m2 = m2;
      this.m3 = m3;
      this.m4 = m4;
      this.m5 = m5;
      this.calcRes();
   }

   void getDetails() {
      System.out.println(this.rollN + " " + this.name + " " + this.m1 + " " + this.m2 + " " + this.m3 + " " + this.m4 + " " + this.m5 + " " + this.total + " " + this.avg + " " + this.grade);
   }

   void calcRes() {
      this.total = this.m1 + this.m2 + this.m3 + this.m4 + this.m5;
      this.avg = (float)(this.total / 5);
      if (this.avg > 90.0F) {
         this.grade = "O";
      } else if (this.avg > 80.0F) {
         this.grade = "A";
      } else if (this.avg > 70.0F) {
         this.grade = "B";
      } else if (this.avg > 60.0F) {
         this.grade = "C";
      } else if (this.avg >= 50.0F) {
         this.grade = "D";
      } else {
         this.grade = "F";
      }

   }
}
